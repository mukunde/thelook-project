# Defensive: these patterns are also in the repo-root .gitignore.
# Kept locally so the module remains self-contained.

target/
dbt_packages/
dbt_modules/
logs/

# Per-user local state generated by dbt
.user.yml
